package com.example.signal.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand / Primary ───────────────────────────────────────────────────────────
val Indigo400  = Color(0xFF818CF8)
val Indigo500  = Color(0xFF6366F1)
val Indigo600  = Color(0xFF4F46E5)
val Indigo700  = Color(0xFF4338CA)
val Indigo100  = Color(0xFFE0E7FF)
val Indigo200  = Color(0xFFC7D2FE)
val Indigo900  = Color(0xFF312E81)
val Indigo950  = Color(0xFF1E1B4B)

// ── Semantic (theme-independent, used for priority/status) ────────────────────
val Rose500    = Color(0xFFEF4444)   // critical / error
val Rose600    = Color(0xFFDC2626)
val Amber500   = Color(0xFFF59E0B)   // high / warning
val Amber600   = Color(0xFFD97706)
val Blue500    = Color(0xFF3B82F6)   // medium / info
val Blue600    = Color(0xFF2563EB)
val Emerald500 = Color(0xFF10B981)   // low / success / done
val Emerald600 = Color(0xFF059669)

// ── Dark scheme neutrals ──────────────────────────────────────────────────────
val Dark950    = Color(0xFF0C0E14)   // deepest background
val Dark900    = Color(0xFF111520)   // screen background
val Dark800    = Color(0xFF181C28)   // card surface
val Dark750    = Color(0xFF1C2033)   // elevated surface
val Dark700    = Color(0xFF222840)   // surface variant / input bg
val Dark600    = Color(0xFF2E3655)   // outline
val Dark500    = Color(0xFF3D4A6A)   // outline variant

// ── Light scheme neutrals ─────────────────────────────────────────────────────
val Light50    = Color(0xFFF8F9FC)   // screen background
val Light100   = Color(0xFFFFFFFF)   // card surface
val Light200   = Color(0xFFF0F2F8)   // elevated surface
val Light300   = Color(0xFFE8ECF5)   // surface variant
val Light400   = Color(0xFFD1D8E8)   // outline
val Light500   = Color(0xFFB8C2D8)   // outline variant

// ── Text neutrals ─────────────────────────────────────────────────────────────
val SlateLight   = Color(0xFF111827)  // primary text on light
val SlateMedL    = Color(0xFF374151)  // secondary text on light
val SlateSubL    = Color(0xFF6B7280)  // tertiary/muted text on light

val SlateDark    = Color(0xFFE4E8F4)  // primary text on dark
val SlateMedD    = Color(0xFFB0BACC)  // secondary text on dark
val SlateSubD    = Color(0xFF8896AC)  // tertiary/muted text on dark
